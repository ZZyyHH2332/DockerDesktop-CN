# 发布草稿 — Docker Desktop 汉化包 4.66.0

- 版本（检测）：4.66.0.222299
- 资产文件：app-Windows-x86-4.66.0.222299.asar.zip
- 仓库路径：./app-Windows-x86-4.66.0.222299.asar.zip
- SHA256: F8797BF255D04C07A34F7B283DDD0CA04BABED9B180A044447215F810DFE1494

安装说明：参见仓库根目录的 `install-zh-windows.ps1` 和 `auto-install-zh.ps1`。

建议：在 GitHub Releases 中为 tag `v4.66.0` 创建一个 Release，并上传上述 `.zip` 作为汉化资产（名称建议 `app-Windows-x86.asar.zip` 或直接 `app-Windows-x86.asar`）。

回滚：如需恢复，请在目标目录使用备份文件 `app.asar.bak.*` 覆盖 `app.asar`。